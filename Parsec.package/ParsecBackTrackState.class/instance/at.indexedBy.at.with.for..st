as yet unclassified
at: inputPosition indexedBy: chart at: index with: aState for: initialState

	state := aState.
	chartIndex := chart.
	stateIndex := index.
	readingPosition := inputPosition.
	initState := initialState
	